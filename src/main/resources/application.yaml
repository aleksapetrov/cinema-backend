spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cinema_backend
    username: postgres
    password: rajan123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: cinema-backend

  ai:
    openai:
      api-key: ${OPENAI_API_KEY:default_key}